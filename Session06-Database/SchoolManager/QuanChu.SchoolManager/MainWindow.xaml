<Window x:Class="QuanChu.SchoolManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanChu.SchoolManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="545" Width="990" Loaded="Window_Loaded" WindowStartupLocation="CenterScreen">
    <Grid Margin="10,0,0,-61">
        <DataGrid x:Name="StudentListDataGrid" HorizontalAlignment="Left" Height="450" Margin="25,29,0,0" VerticalAlignment="Top" Width="915" AutoGenerateColumns="False">


        
        <DataGrid.Columns>
            <DataGridTextColumn Header="Mã sách" Width="50" Binding="{Binding BookId}" />
                <DataGridTextColumn Header="Tên sách" Width="150" Binding="{Binding BookName}"/>
                <DataGridTextColumn Header="Mô tả" Width="200" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Năm xuất bản" Width="100" Binding="{Binding PublicationDate}"/>
                <DataGridTextColumn Header="Số lượng sách" Width="70" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Đơn giá " Width="70" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Tác giả" Width="120" Binding="{Binding Author}"/>
                <DataGridTextColumn Header="Cate FK" Width="70" Binding="{Binding BookCategoryId}"/>
                <DataGridTextColumn Header="Cate name" Width="120" Binding="{Binding BookCategory.BookGenreType}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
